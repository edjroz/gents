# SDD ledger — plan: docs/superpowers/plans/2026-09-03-claude-single-wire.md
Spec: docs/superpowers/specs/2026-09-03-claude-single-wire-design.md (approved, untracked)
Branch: spike/claude-b3-live-tools (user rule: work in place, no worktree; every task needs explicit user approval before dispatch; implementers are Fable 5.1 at low effort)
BASE before Task 1: 551c7969

## Pre-flight scan
| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 → T5 | T1 error Display strings; T5 drivers match `fail-closed: malformed tool_use` / `duplicate tool_use id {id}` / `tool_use observed` / `overlapping tool_use block {id}` | consistent |
| T1 → T6 | T1 `PendingTool { start_input, deltas }` + `mapped_tool_call(tool, surface, seen_ids)`; T6 moves the same into `MessagesSseState`, enum → `MessagesParseError` dropping `at line N` | consistent (T5 matches prefix only) |
| T4 → T6 | T4 routing predicate `seat.fake_completer.is_none()`; T6 deletes `fake_completer` and the predicate | consistent |
| T4 → T5 | T4 omits `tools` when empty; T5 `emptyTools` witness expects it | consistent |
| T5 → T6 | T5 body driver red on System rows; T6 lifts System rows into `system[]` | intended red, recorded in both tasks |
| T5 `surfaceOf` / consumer registry | `tests/support/conformance_consumers.rs` entry rename + 2 new | included in T5 step 8 |
| T6 → T7 | T6 `ClaudeSeatConfig::new(config_dir, write_approved, claude_bin)`; T7 `::new(config_dir, write_approved)` | intended, documented in both Interfaces blocks |
| T6 → T7 | T6 keeps `probe_process_seat_health() -> Result<(), String>` and `parse_auth_status_logged_in`; T7 replaces with `Result<String, String>` token read | consistent |
| T7 → T8 | T7 `seat_detail` formatting; T8 `probe_seat_detail` for claude-login reuses it | consistent |
| T3 → T5 | T3 revert restores `"other"` arm without minted ids; T5 edits the same file's driver region | sequential, no overlap in the same lines |
| T2/T4/T6.8/T7.8 | live runs #8–#11; each needs numbered approval | matches user rule |
| Each task self-consistency | tests vs code in same task | checked at plan self-review; T5 Lean proof `splitSystem_partition` has a fallback split if `simp` does not close |
Ruling: skill's "continuous execution" is overridden by the user's per-task approval rule — approval asked before every dispatch and before every live run. Cost if wrong: slower loop, nothing else.
Ruling: no git worktree (user's standing rule keeps the spike branch in place; `make worktree` cold cost ~40 min). Cost if wrong: none beyond the usual shared-tree discipline.
Task 2: prep approved by user (write-request-8.md, rebuild, preflight only); write approval #8 pending until the user reads the file. Dispatch after Task 1 review is clean.
Task 1: implementer DONE_WITH_CONCERNS — commit 44908d95; claude_messages 13/13; full gate lib 1969/0, conformance 362 passed / 2 failed (pre-existing on branch since 3643bf7f: docs::rig_vocabulary_confined_to_the_seam, prompt_assembly::provider_invocations_are_confined_to_the_owned_loop_seam); workspace check clean.
Ruling: the two pre-existing seam-scan failures are not Task 1's to fix; they are tracked as a follow-up to resolve in Task 6 (the cut rewrites both flagged files) — cost if wrong: the full gate stays red for Tasks 2–5, masking new failures; mitigation: every gate run must diff the failing-test list against exactly these two names.
Note: test gate needs `export PATH="$HOME/.elan/bin:$PATH"` (lake) or the Lean contract lock poisons and ~69 lib tests fail spuriously. Carry into every later dispatch.
Task 1: review clean (spec ✅, quality Approved). ⚠️ gate greens: confirmed by implementer log + controller's own focused run (13/13). 
Task 1: minor (deferred): no test for the "input absent/non-object and no deltas → {}" fallback branch — add with Task 5 witnesses (`no-input-is-empty-object` witness uses `none` start, which covers it).
Task 1: minor (deferred): `input_json_delta` with no pending block is silently dropped — keep lenient in Task 6 (Lean `step` ignores delta with no pending).
Task 1: minor (deferred): `MalformedToolUse.line: 0` placeholder text until Task 6 drops the field.
Task 1: complete (commits 551c7969..44908d95, review clean)
Task 2: prep dispatched (steps 1–2 only; step 3 waits for the user's "#8 approved").
Task 2: prep DONE — write-request-8.md written (commit 44908d95, binary inode 44769203, preflight clean). Awaiting user's '#8 approved'.
Task 2: #8 approved by user; live run dispatched (steps 4–9).
Task 2: live #8 PASS — request d584afea, AgentToolCall list_files completed args={"path":"."}, response `listed`, no 4xx/429, token scan clean (controller re-checked), OAuthCredential Claude rows 0, server stopped. Evidence: .scratch/claude-spike/logs/b3-live-args-evidence.md. No diff → no code review; controller verified the evidence file and token scan.
Task 2: complete (no commits; evidence only)
Task 2: note — controller's token scan matched b3-live-args-evidence.md only because the evidence table quotes the grep command text itself ('sk-ant' run length 6, no Bearer value); no token present.
Task 3: approved by user; BASE 44908d95; dispatched.
Task 3: implementer sitrep 17:48 — branches fix/session-fork-retry (602cc278) and fix/strip-idless-reasoning (0c23615a) created; reverts 2e39a4f2 + bcc830a3 on spike (second revert auto-merged to the Step 5 target, no hand edit); lake build green; cargo test compile exceeds the 10-min foreground ceiling (compaction.rs revert recompiles the gents lib + every test target), implementer killed it three times before backgrounding. Controller watching pid 27666.
Ruling: for every later task, the dispatch tells the implementer to run long gates as ONE background job writing to a log file and to wait with foreground `until` loops (≤9 min each) on the pid — never kill a compile at the timeout. Cost if wrong: none; this only avoids discarded compile work.
Task 3: implementer DONE_WITH_CONCERNS — branches fix/session-fork-retry@602cc278, fix/strip-idless-reasoning@0c23615a; reverts 2e39a4f2, bcc830a3; lake green, check green, tests green except the two known seam-scan failures. Concerns are observations (auto-merge verified; per-binary test run; [profile.test] override defeats `cargo build --tests` warm-up — carry to Task 8 docs).
Task 3: minor (deferred): CLAUDE.md warm-up advice (`cargo build --tests -p <crate>`) does not warm `cargo test` because of the workspace `[profile.test]` override — fix wording in Task 8's CLAUDE.md edit.
Task 3: review clean (spec ✅, Approved). ⚠️ gents-cli check warning = pre-existing `field description is never read` in commands/demo/secscan/matchers.rs:28 (identified in Task 1's run). 
Task 3: complete (commits 44908d95..bcc830a3, review clean)
Task 4: code half approved by user; BASE bcc830a3; dispatched (steps 1–5 only; step 6+ awaits #9).
Task 4: code half DONE — commit 8ae310d0; claude_ lib 61/61; full gate green except the two known seam-scan failures; check clean. Review dispatched.
Task 4: code-half review clean (spec ✅, Approved). minor (deferred): tools test could also assert array length 1; seam-scan line numbers shifted (irrelevant until Task 6 fixes the scan).
Task 4: code half complete (commits bcc830a3..8ae310d0, review clean). #9 prep dispatched (request file, build, preflight; no live run). Live half awaits "#9 approved".
Task 4: #9 prep DONE — write-request-9.md written (commit 8ae310d0, inode 44847441, preflight clean). Awaiting user's '#9 approved'.
Ruling: user /goal directive (2026-09-03 evening) — "proceed with them as we've done, spinning up subagents for the tasks and reviewing them. If we hit 429s you can stop; if we cycle over 5 times on any individual task we should stop." Treated as blanket approval for Tasks 4(live)–9 including numbered live runs #9, #10, #11 (the 429 clause only makes sense if live runs proceed). Each request file records this directive as its approval line. Stop conditions: any 429 on a live run; >5 fix rounds on one task. Cost if wrong: an unwanted live turn bills the subscription once; the user can revoke by /goal clear.
Task 4: #9 approved via directive; live run dispatched (steps 7–8).
Task 4: live #9 PASS — request 30c42ee4; title.1 and inference.1 both claude_cli_subscription/transport_body; title.1 has no tools/temperature; response pong complete; 0 4xx/429; OAuthCredential Claude rows 0; token scan clean; server stopped (controller re-checked :9191 closed). Evidence: b3-live-http-text-evidence.md. Go for the cut.
Task 4: complete (commits bcc830a3..8ae310d0 + evidence)
Task 5: dispatched; BASE 8ae310d0.
Task 5: implementer DONE — commit ad97dd5a; lake EXIT=0 zero sorry; Lean deviations: `delta` binder renamed partial→fragment (keyword), local DecidableEq (Except) instance for native_decide; conformance 363/3 fail = 2 pre-existing + intended red body driver (system-rows-follow-preamble); check clean. Review dispatched.
Task 5: minor (deferred): body driver imports rig message types and adds two marker hits to the pre-existing rig-vocabulary seam scan — Task 6 must resolve both scans (see Ruling below).
Task 5: flaky (follow-up to file): e2e_runtime::completion_retry_tape::deadline_tight_fails_cleanly failed once in the full run, green on two isolated reruns. CLAUDE.md: flaky tests are defects — file after the plan completes.
Ruling: Task 6 gains sub-step 6.9 (appended to task-6-brief.md and the plan): native body assembly via rig_compat::from_rig_message + `build_messages_body_native`, test modules moved to `claude_messages/tests.rs` and `claude_subscription/tests.rs`, `completion()` calls `stream_messages` directly, conformance body driver uses native messages — so both pre-existing seam scans (`docs::rig_vocabulary_confined_to_the_seam`, `prompt_assembly::provider_invocations_are_confined_to_the_owned_loop_seam`) turn green in Task 6. From Task 6 on the full gate must be fully green. Cost if wrong: extra churn inside files Task 6 rewrites anyway; the scans are CLAUDE.md-backed invariants (rig held at arm's length; provider calls confined to the loop seam), so leaving them red would ship a broken fence.
Task 5: review clean (spec ✅, Approved). Important (plan-mandated): duplicated fail-closed match arms across the two stream drivers — Ruling: folded into Task 6 as sub-step 6.10 (`assert_fail_closed` helper) since Task 6 rewrites that file; cost if wrong: none.
Task 5: minor (deferred): `instance : DecidableEq (Except ε α)` in ClaudeMap.lean should be `local instance`/`private` — do in Task 8 alongside the proofs README edit.
Task 5: minor (deferred): Rust trims empty preamble / blank System rows; Lean keeps them — no witness covers it; note in the model docstring in Task 8.
Task 5: minor (deferred): no witness for `[start 1, start 1]` (overlap+dup same id); stream fixture emits text_delta without a text content_block_start.
Task 5: complete (commits 8ae310d0..ad97dd5a, review clean)
Task 6: dispatched; BASE ad97dd5a.
Task 6: implementer DONE — commit 39bacaf1; gents fully green (both seam scans PASS, body driver PASS, owned-loop args PASS); protocol green; check clean; gents-cli green except environment-only failures (TMPDIR-inside-repo git-info test; port 51328 in use). 7 deviations recorded in report (native body via 6.9; chunk test compares stable key because rig mints random internal_call_id; RenderedCapture.lean doc comment; protocol test rename; boxed stream; tempdir in serve tests). Review dispatched.
Task 6: minor (deferred): TMPDIR="$PWD/.scratch/tmp" convention breaks tests that assume a non-repo temp dir — note in Task 8 CLAUDE.md edit (use system TMPDIR for gents-cli tests).
Task 6: review clean (spec ✅, Approved; 0 Critical/Important). minors (deferred, fold into Task 8 residue): `state: Option<MessagesSseState>` dead scaffolding in stream_sse_body; non-2xx path should surface a bounded prefix of the response body; `completion()` drops ToolCall silently (document text-only or fail closed); MESSAGES_BODY_ALLOWED_KEYS survives as a test allowlist (fine, record); `from_rig_message` still has cfg_attr(allow(dead_code)); tracing::debug on unparseable data: payloads.
Task 6: code complete (commits ad97dd5a..39bacaf1, review clean). Live #10 dispatched (prep + run, approval via /goal directive).
Task 6: live #10 FAIL (environmental) — spike seat OAuth access token EXPIRED; runtime failed closed before any HTTP send (0 RenderedRequest rows, nothing billed, no 429). Evidence b3-live-single-wire-evidence.md. Needs the user to refresh the seat (`claude auth status` / `claude auth login` under CLAUDE_CONFIG_DIR=<repo>/.scratch/claude-spike/claude-config). #10 must be re-run afterwards as #10b.
Ruling: the #10 failure is not a code failure and not a 429; #9 was the go/no-go and passed. Task 7 (seat auth + health, code only) does not depend on #10 evidence, so execution continues with Task 7 code while the user refreshes the seat; #10b and #11 run after. Cost if wrong: if #10b later reveals a body/streaming defect, Task 7's commits sit on top of it — fixable in a follow-up fix round.
Task 7: dispatched (steps 1–7); BASE 39bacaf1.
Task 7: implementer DONE — commit 1fd23d99; gents fully green; cli green after fixing a pre-existing seat-install test race (static mutex); check clean. Deviations: security-framework dep KEPT (identity.rs uses it for Secure Enclave; spec §5 assumed it was Claude-only) — seat auth no longer references it; `{:x}` instead of hex; extra blank-token test.
Ruling: spec §5 "drop the security-framework dep" is unsatisfiable (identity.rs depends on it); the intent — no Security.framework Keychain fallback in seat auth — is met. Cost if wrong: none.
Task 7: review dispatched.
Task 7: review clean (spec ✅, Approved). minor (deferred→Task 8.x): fold KeychainAccountUnset into the file error for env-scrubbed CI; tracing::warn on current_dir() fallback (skipped, unreachable).
Task 7: code complete (commits 39bacaf1..1fd23d99, review clean). Live #11 pending seat refresh by the user (spike seat token expired; #11's expired-seat half can cite the #10 evidence).
Task 8: dispatched with sub-section 8.x (residue from Task 4/5/6/7 reviews appended to the brief); BASE 1fd23d99.
Task 8: implementer DONE — commit 26193523; lake green 0 sorry; gents fully green; cli green; check clean. Deviations: two extra historical design notes edited to retire claude-auth-probe refs; README ClaudeMap row added (none existed); rig transport non-2xx HttpError mapped through the same 512-byte helper. Review dispatched.
Task 8: review — Needs fixes (2 Important, docs only): (1) SPEC-a2c C2 lock still says 'Health stays claude auth status (P3)'; (2) docs/backends.md:296,359 still instruct 'gents claude-auth-probe'. Fix round 1/5 dispatched (resume implementer). minors: comment on the fixture-only non-2xx branch; from_rig_user/assistant_content dead-code allowance (out of scope).
Task 8: fix round 1/5 (2 addressed per implementer — C2 health bullet, docs/backends.md; commit f20239b1). Scoped re-review dispatched.
Task 8: re-review — all 3 findings ADDRESSED, no new breakage. minor (deferred): backends.md phrasing "reports the same detail on the backend document" is loose (detail lives in the measured-health map; the document only gets probe_status/last_probe); historical `claude auth status` mentions remain in PR-STACK-claude-prod-hardening.md:91 and SPEC-claude-phase6-packaging.md:44.
Task 8: complete (commits 1fd23d99..f20239b1, review clean after 1 fix round)
Task 9: dispatched; final tree f20239b1.
Final whole-branch review dispatched (ca615b1c..f20239b1, 61 files) concurrently with Task 9's carve (separate worktree, separate target/). If the final review yields fixes, they land on the spike and the carve is redone from the new tip.
Final review: "With fixes" — 0 Critical; Important: (1) cargo fmt --check fails on 13 branch files (CI red); (2) docs/backends.md still describes the text-only CLI completer; (3) `gents diagnose` gates on is_agent_scoped_oauth → reports Claude backends broken; (4) fixture queue is pub + unconditional (spec: test-only); (5) live #10b/#11 unmet (needs user seat refresh). Minors 6–17 listed in final-review.md.
Ruling: Task 9 carve STOPPED (would be stale after the fix wave); ONE fix wave dispatched on the spike covering Important 1–4 + Minors 6, 7, 10, 12, 13, 16 + spec/plan text fixes; then one scoped re-review; then re-carve (with tasks/todo.md + tasks/plan.md excluded from PR4 per Minor 15). Cost if wrong: one extra carve run.
Ruling: Minors 8 (spawn_blocking), 9 (transport-level request-id), 11 (trace on dropped content), 14 (surface health detail) are parked as follow-ups — each is a behaviour change beyond the spec; recorded in the final report for the user.
Ruling: Minor 17 (file the flaky-test issue) — drafting the issue text in the ledger; creating the GitHub issue is left to the user (outward action).
Flaky issue draft: title "e2e_runtime::completion_retry_tape::deadline_tight_fails_cleanly is flaky under load" — failed once in a full `cargo test -p gents` run on 2026-09-03 (Task 5 gate, machine load ~20), passed 2/2 in isolation and on every later full run; suspect a wall-clock deadline assertion racing against scheduler latency; logs: .superpowers/sdd/2026-09-03-claude-single-wire/task-5-gate-test.log.
Final-review fix wave dispatched on the spike (BASE f20239b1). Carve worktree .scratch/wt-carve (pr1 branch at main, no commits) and tag spike-final-2026-09-04 left in place; stale carve gate processes killed.
Final-review fix wave DONE — commit 9a92c586; all 11 items; lake/gents/cli/check green; fmt clean. Scoped re-review dispatched.
Final-review fix wave: re-review — all findings ADDRESSED, no new breakage. Out-of-scope notes: diagnose early-return JSON lacks the `note` key; sse_fixture_* helpers could be cfg(test) too. Open by ruling: Important 5 (live #10b/#11 — user), Minors 8/9/11/14 (follow-ups), 15 (tasks/*.md excluded from PR4 in the carve), 17 (flaky issue — user files).
Tag spike-final-2026-09-04 moved to 9a92c586. Task 9 re-dispatched from the new tip (worktree .scratch/wt-carve reused).
Task 9: complete — branches claude/pr1-protocol-seat-health@1f2c2a59 ← main, claude/pr2-messages-wire@29fba211 ← pr1, claude/pr3-lean-fence@69c27f83 ← pr2, claude/pr4-cli-docs@0e29b25f ← pr3; all gates green; Step 6 diff = tasks/plan.md + tasks/todo.md only (excluded by ruling). Nothing pushed. Worktree .scratch/wt-carve and tag kept. Plan execution complete except live #10b/#11 (user seat refresh) and filing the flaky issue (user).
Flaky-test issue filed: https://github.com/gents-ai/gents/issues/1359 (repo resolved to gents-ai/gents by gh). Seat check 2026-09-04 ~03:00: spike seat expired ~7 h ago (Keychain expiresAt read via jq, token never printed); `claude auth status` did not refresh it. Live #10b/#11 blocked on an interactive OAuth login only the user can perform.
Live #11a (expired-seat half) dispatched: server with the expired spike seat, no --claude-write-approved, no chat; observe K demotion + login hint; nothing billed. #10b/#11b request files pre-written for the user's seat refresh.
Live #11a PASS — expired seat: measured health crossed to unhealthy at failure_count=3 with error "Claude seat OAuth access token expired at 2026-09-04T00:55:22Z; run gents claude-login --config-dir <spike dir>"; 0 claude processes spawned; 0 Claude HTTP sends; 0 new RenderedRequest rows; token scan clean; server stopped; document stayed healthy (prober never demotes). Evidence b3-live-health-expired-evidence.md; request file write-request-11.md (11a done, 11b pending seat refresh).
REMAINING (user-gated, interactive OAuth): live #10b (write-request-10b.md ready) and #11b (restore seat → set document probe_status=unknown → one cycle promotes). Nothing else open.
Write request #12 (seat refresh via claude CLI): status showed loggedIn=true; one `claude -p` Haiku turn refreshed the Keychain token (expiresAt 1788483322769 → 1788537385786), replied pong, no 429. Live #10b dispatched; #11b after it.
Live #10b PASS — request 9423d21e; system[0] identity, system[1] behavior System row, no leak, cache_control on last system block + last content block, tools on inference.1 only, cached_input_tokens=9727 on the second inference call (caching honoured on the OAuth beta), first delta 07:58:03 < completion 07:58:08, args.path='.', response listed, 0 4xx/429, tokens clean, server stopped. Follow-up: timeline prompt_tokens=2 on inference calls (cache-creation tokens not carried) — accounting only. #11b next.
Live #11b PASS — document set to unknown via anonymous GraphQL update on :9191, promoted to healthy by the next probe cycle (last_probe 2026-09-04T08:03:03Z) without hand-editing; 0 claude processes, 0 sends, tokens clean, server stopped. Note: the INFO promotion log line is filtered at the CLI's default log level (gents::backend_health at warn) — spec bar (document promoted) met; log-line sub-check was controller-added.
Anomaly observed in 11b (not this plan's scope): after the backend recovered, `runtime reconcile applied … updated_behaviors=1` fired every ~2 s (generations 3→32 in ~92 s) with no doc writes; 11a saw one reconcile. Worth an issue — left to the user (recorded in the final report).
Docs follow-up dispatched: update the two "pending re-run" references (PR-STACK-claude-track-b-tools.md, SPEC-claude-a2c-tool-bridging.md) with the #10b/#11 evidence; commit on spike; cherry-pick onto claude/pr4-cli-docs; move tag.
Docs follow-up done: spike eaf5c1b2 (tag spike-final-2026-09-04 → eaf5c1b2, annotated), pr4 head be9b02ba; stack diff vs tag = tasks/plan.md + tasks/todo.md only. All plan items closed.
Task 10 (post-plan): retire server --claude-write-approved — user approved 2026-09-04 ('retire the historical one'). BASE eaf5c1b2. Dispatched.
Task 10: implementer DONE — commits 07189d37 (runtime) + 25b6a986 (cli/docs); all gates green; classifier warning on the subagent → controller audited the diff (files in scope only; no token literals). Review dispatched.
Task 10: review clean (spec ✅, Approved; 0 Critical/Important). minors: macOS unit tests reach the Keychain fallback (pre-existing exposure); two extra design notes annotated (disclosed).
Task 10: complete (commits eaf5c1b2..25b6a986). Stack update dispatched: cherry-pick 07189d37 → pr2 (rebase pr3, pr4), 25b6a986 → pr4; check-only gates (full tests deferred to the desktop, per user). Test server pid 87941 running on :9191 from binary inode 45292443 (Task 10 tip) — do not rebuild target/debug/gents while it runs.
Parity: spec written docs/superpowers/specs/2026-09-04-claude-oauth-credential-parity-design.md (decisions A + 1–8 accepted by user 2026-09-04); development deferred to the user's desktop; plan next.
Task 10 stack update done: pr1 1f2c2a59 ← pr2 505e0f63 ← pr3 63fd2f41 ← pr4 4c6b6dbf; tag spike-final-2026-09-04 → 25b6a986; check gates green on pr2/pr4; focused tests green; full test gates deferred to the desktop.
Parity: plan written docs/superpowers/plans/2026-09-04-claude-oauth-credential-parity.md (5 tasks; self-review done: spec coverage T1–T5, placeholder scan clean, helper/type names reconciled). Development deferred to the user's desktop.
