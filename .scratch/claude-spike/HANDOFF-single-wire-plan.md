# Handoff: Claude single-wire — write the implementation plan, then execute

**Date:** 2026-09-03
**For:** a new Claude Code session (Fable 5.1) in `/Users/edjroz/Repos/source/gents`
**Branch:** `spike/claude-b3-live-tools` (41 commits ahead of `main`; uncommitted: `crates/gents/src/claude_messages.rs` identity block + 3 tests — leave uncommitted unless asked)
**Previous session:** `219f444a-6c4c-4bc6-a72e-42971284e822` (ran out of context mid-plan; full transcript at `~/.claude/projects/-Users-edjroz-Repos-source-gents/219f444a-6c4c-4bc6-a72e-42971284e822.jsonl` if you need a verbatim detail)

## Standing rules (from the user, verbatim where quoted)

- "any further coding changes required must be approved before happening and launched in a subagent fable 5.1 unless specified otherwise." → Propose the change, wait for an explicit yes, then `Agent` with `model: "fable"`. Never edit source from the main session.
- Live Claude runs need an **explicit numbered write approval in the current session** (next is **#8**). Write `.scratch/claude-spike/logs/write-request-N.md` first. If 429 again: stop, file evidence, do not hammer.
- Never print OAuth tokens / `sk-ant-oat01` / `Bearer` values. Never upsert `OAuthCredential`.
- Do not restore Grok as default. Default behavior model stays `claude-sonnet-5` (`did:key:z6MkpEq4KsC2oibBmHe6faw1246CU5gmtVJJEYf3wvo1yKiQ:default`, backend `…:claude-backend`).
- Do not rustfmt `crates/gents/src/lib.rs`. Leave dirty `docs/design-notes/SPEC-claude-a2b-in-process.md`, `TODO.md`, `tasks/a2b2-interjection-plan.md` alone.
- Prefer `./target/debug/gents`. Build env: `export GENTS_SKIP_LENS_BUILD=1 GENTS_SKIP_CALLBACK_WASM_BUILD=1 TMPDIR="$PWD/.scratch/tmp"`.
- The spec and the plan are **not committed** ("we are not writing this to the version contorl so don't commit it").
- Context budget is 200k: read line ranges, not whole files; delegate broad reads to `Explore` subagents; write the plan file in sections (Write header, then Edit-append per task) rather than one giant Write.

## Where we are

1. Track B3 done: identity block `You are Claude Code, Anthropic's official CLI for Claude.` as `system[0]` clears the OAuth-routing 429 (write request #7 PASS; `.scratch/claude-spike/logs/b3-live-identity-evidence.md`). Caveat: live `tool_use` arrived with `arguments: {}` — bug C1 below. Next live bar must assert arguments.
2. Review done: `.scratch/claude-spike/logs/review-track-ab-2026-09-03.md` (C1–C10 + ponytail). Read it first; it is short.
3. Brainstorm done → spec **approved**: `docs/superpowers/specs/2026-09-03-claude-single-wire-design.md` (275 lines; §1 decisions, §2 target arch + Removed list, §3 Lean, §4 body+streaming, §5 seat/health/promotion/CLI, §6 testing + live probes #8–#11, §7 sequencing, §8 PR stack, §9 constraints, §10 out of scope). Read it fully — the plan argues from it.
4. User said **"proceed witht his spec"** → `superpowers:writing-plans` was invoked; research finished; **the plan file has not been written yet**.

## Your immediate task

Invoke `superpowers:writing-plans` and write `docs/superpowers/plans/2026-09-03-claude-single-wire.md` (do not commit). Header: Goal / Architecture / Tech Stack / Spec path / Global Constraints (copy spec §9 verbatim). Tasks follow spec §7 order; each task: Files (exact paths:lines), Interfaces, TDD checkbox steps with real code, run commands, commit step. No placeholders. Then self-review (spec coverage, placeholder scan, type consistency) and offer: **1. Subagent-Driven (recommended)** / **2. Inline**. Whatever the user picks, every code edit still goes approval → Fable 5.1 subagent.

## Plan design decisions already made (not in the spec — record them in the plan)

- **Fixture queue.** One SSE fixture cannot serve a two-turn owned-loop test. Replace `install_messages_sse_fixture(Option<String>)` with `install_messages_sse_fixtures(Vec<String>)` backed by `Mutex<VecDeque<String>>`; `stream_messages` pops one per call; `lock_process_seat_for_test()` clears it. Empty queue with no live seat → ProviderError.
- **Two witness sets instead of the spec's four ledger names.** `PromptAssemblyClaudeBodyCases` (snapshot key `prompt_assembly_claude_body_cases`; covers `systemAssembly` + `emptyTools`; drives `claude_messages::build_messages_body`; witness carries `preamble : Option String`, `rows : List String`, `tools : List String`, expected `system : List String`, `toolsPresent : Bool` — the identity constant is fenced by the expected `system[0]`) and `PromptAssemblyClaudeStreamCases` (`prompt_assembly_claude_stream_cases`; covers `accumulate` + `overlappingBlock`; drives `claude_messages::parse_messages_sse` with events rendered as SSE `data:` lines; outcome tags `ok` with `(id, argsJson)` list, `emptySurface`, `unmappedName:`, `duplicateId:`, `overlappingBlock`). Existing `PromptAssemblyClaudeMapCases` driver test `generated_claude_map_cases_drive_the_completer_parser` is re-pointed at `parse_messages_sse` (rename to `..._drive_the_messages_parser`). State this naming deviation from the spec explicitly in the plan.
- **Lean stream model sketch** (`Proofs/PromptAssembly/ClaudeMap.lean`, append after `mappedKind`): `inductive StreamEvent | text (t : String) | start (id : ToolCallId) (name : String) (input : Option String) | delta (partial : String) | stop`; `def accumulate (start : Option String) (deltas : List String) : String := match deltas with | [] => start.getD "{}" | _ => String.join deltas`; theorems `accumulate_ignores_start_when_streamed (h : deltas ≠ [])` and `accumulate_uses_start_when_no_deltas`; `structure Pending where id name (start : Option String) (deltas : List String)`; `structure StreamState where pending : Option Pending, seen : List ToolCallId, out : List (ToolCallId × String)`; `flush` (dup check `p.id ∈ st.seen` → `.duplicateId`, then `mapToolUse`, append `(id, accumulate p.start p.deltas.reverse)`); `step` (`start` while pending → `.error .overlappingBlock`; `delta` with no pending ignored; `stop` → flush); `runStream (surface) (events) := (events.foldlM step init) >>= flush`. `MapError` gains `| overlappingBlock` with `errorName = "overlappingBlock"`. Left-to-right, first error wins — matches Rust streaming order (note: existing `mapPairs` recurses on the tail; keep it, the new model is separate).
- **`splitSystem`** needs a local row type because `Transcript.MessageRole` has no System: `inductive Msg | system (text : String) | other (tag : String)`; `splitSystem : List Msg → List String × List Msg`; `identity : String := "You are Claude Code, Anthropic's official CLI for Claude."`; `systemBlocks (preamble : Option String) (rows : List String) : List String := identity :: (preamble.toList ++ rows)`; theorems `systemBlocks_head`, `systemBlocks_tail_verbatim`, `splitSystem_partition`. `toolsField : List String → Option (List String)` = none on `[]`.
- **`surfaceOf`** in `Conformance/ContractCases/PromptAssembly.lean` becomes `names.toFinset`.
- **Creep split:** `git branch fix/session-fork-retry main && git cherry-pick 0b89e688` (fork retry; 7 files, no later commit touches them) and `git branch fix/strip-idless-reasoning main && git cherry-pick 63ff2ff3`; then `git revert` both on the spike. `63ff2ff3` conflicts in `crates/gents/tests/conformance/prompt_assembly.rs` (later touched by `fac2a94d`): resolve by keeping the ClaudeMap driver and dropping the `.with_id(format!("rs-lean-{}", item.value))` minting in the `"other"` arm (~L100-106) back to `AssistantContent::Reasoning(Reasoning::new(&reasoning_body(item.value)))`.
- **`inference_backend.rs`**: `git checkout main -- crates/gents/src/config_client/inference_backend.rs` (spike's clear-fields helpers have no external callers).
- **Protocol seam:** `CaptureSeam` back to single `TransportBody`; `build_rendered_completion_request_at_seam` collapses back to main's `build_rendered_completion_request` (no seam param). Persisted spike rows with `"capture_seam":"process_cli"` stop parsing — acceptable, never shipped.
- **Health promotion:** Claude branch in `backend_health.rs` falls through to shared recording; `record_probe_event` loses the `promote_document_on_unknown` param; `cycle_marks_claude_healthy_when_auth_status_logged_in` flips to assert promotion.

## Task outline for the plan (spec §7 order)

1. **C1 fix** in `claude_messages.rs`: `PendingTool { id, name, start_input: Option<String>, deltas: String }`; at stop use `deltas` if nonempty else `start_input` else `{}`; unparseable → `CompleterParseError::MalformedToolUse` (fail closed, no silent `json!({})`); add dup-id check (C8) → `DuplicateToolUseId`; overlapping `content_block_start` while pending → `MalformedToolUse`. Tests: `sse_tool_use_deltas_yield_exact_arguments`, `sse_tool_use_without_deltas_uses_start_input`, `sse_tool_use_with_unparseable_input_fails_closed`, `sse_duplicate_tool_use_id_fails_closed`.
2. **Live #8** (numbered): argument-asserting bar — `list_files` with `"."`; query `AgentToolCall { tool_name args lifecycle_state }`; `args` must contain the directory. Procedure = `write-request-7.md` (server `--home ~/.gents --tool-root <repo> --tool-ceiling readwrite --claude-config-dir <repo>/.scratch/claude-spike/claude-config --claude-write-approved`; one `gents chat` fresh session; `gents trace timeline`; stop server; evidence file `b3-live-args-*`).
3. **Creep split** (above).
4. **Live #9** after routing switch to always-HTTP (one-line change in `ClaudeSubscriptionModel::stream`): text-only turn + title generation on HTTP.
5. **Lean slice**: ClaudeMap extensions; witness structs in `Conformance/ContractCases/PromptAssembly.lean` (after L555, before `end`); JSON in `Contracts/Json/PromptAssembly.lean` (helpers `jsonString`, `jsonStringArray`, `jsonArray`, `boolString`; `Helpers.lean` has `jsonOptionalStringArray`, `jsonOptionalNat`, `jsonOptionalBool`); Snapshot keys `Contracts/Json/Snapshot.lean` ~L247; ledger `CoverageLedger.lean` ~L995 (pattern: `tagged (consumerCoverage "prompt_assembly_cases" "<Set>" "conformance::prompt_assembly::<test>") "prompt-assembly" [Surface.runtimeInternal]`); Rust `lean_vocab_test/prompt_assembly.rs` structs (existing `LeanPromptAssemblyClaudeMapCase` ~L75-84), `support.rs` field ~L169 + accessor ~L1013; `tests/conformance/coverage.rs` emitted set ~L817-822; driver tests in `tests/conformance/prompt_assembly.rs` (existing driver L325-422). `lake build` zero sorry; snapshot regenerates at test time via `gents_lean_contract::load_contract_snapshot()`.
6. **Single-wire cut**: `stream()` always → `stream_messages`; `build_messages_body` system = `[identity, preamble?, Message::System rows…]` with `cache_control: {type: ephemeral}` on last system block and on last content block of last message; `tools` key absent when empty; `MessagesSseState { pending, seen_ids, usage, surface }` with `push_line(&str) -> Result<Vec<RawStreamingChoice<ClaudeStreamResponse>>, CompletionError>` + `finish()`; `futures::stream::try_unfold` over the body with a line buffer (chunk-boundary test); `event: error` payload → ProviderError `"{type}: {message} (request-id …)"`; one `Arc<reqwest::Client>` on `ClaudeSeatConfig { config_dir, write_approved, http }`; drop `ClaudeMessagesTransport`, `MESSAGES_BODY_ALLOWED_KEYS` debug_assert; delete `spawn_completer`, `stream_child_stdout`, `flatten_completion_request`, JSONL parsing + `fixtures/` in `claude_completer/` (keep `DEFAULT_MODEL_ID`, `STRIPPED_ENV_VARS`, `sanitize_child_env` for `claude-login`), `PATH_A_MODEL_IDS`, `live_claude_allowed` (write gate check moves into `stream_messages` on `seat.write_approved` unless fixture queued), `parse_auth_status_logged_in`, `claim_and_capture_process_cli` + `CaptureSeam::ProcessCli` + their tests; rewrite `agent/loop_stream/tests/claude.rs` onto two SSE fixtures (turn 1 `tool_use echo` with `input_json_delta` `{"text":"hi"}`, turn 2 text `done`), assert `AgentToolCall.args` contains `"hi"`; consolidate helpers: one `install_fake_seat()`, one `ping_request()`, one `pub(crate) fn sse_fixture_tool_use(id, name, partial_json)`; no hardcoded home paths (`env!("CARGO_MANIFEST_DIR")`-relative or tempdir). Seat-literal copies to update: `claude_subscription.rs` ~L665, 761, 817, 875, 1023, 1069, 1147, 1179, 1209, 1248, 1340; `backend_health.rs` ~L730; `loop_stream/tests/claude.rs` L51. Then **live #10**.
7. **Seat auth + health**: drop `read_macos_keychain_via_framework`, `ERR_SEC_ITEM_NOT_FOUND`, `security-framework` dep (`crates/gents/Cargo.toml` L83-84); error variants → `MissingFile{path}, Io, Malformed, KeychainNotFound, KeychainAccessDenied, KeychainAccountUnset, Expired{expires_at}`; digest test computes expectation from a relative path instead of the hardcoded `/Users/edjroz/...` → `6b2dc9d7`. Health: probe = `read_seat_access_token(&seat.config_dir)`; Ok → Healthy detail `source=file|keychain expires_at=<rfc3339>`; Err → Unhealthy with Display; `Expired`/`MissingFile` append `run gents claude-login --claude-config-dir <dir>`; promotion as above. Update the three Claude tests in `backend_health.rs` (~L741-830). Then **live #11**.
8. **CLI/docs trim**: `args.rs` remove `Command::ClaudeAuthProbe`/`ClaudeAuthProbeArgs` (L93-98, L557-566), `ClaudeLoginArgs.console/sso` (L546-554), serve flags `--claude-workdir/--claude-log-dir/--claude-bin/--claude-fake-completer` (L851-881; keep `--claude-config-dir`, `--claude-write-approved` with `requires = "claude_config_dir"`); `args/tests.rs` L785-835, L877-946; `commands/serve.rs` `validate_claude_seat_args` L82-107, `install_claude_subscription_seat` L109-137, calls L453-454, status JSON L859-874 (keep `claude_seat`, drop `claude_fake_completer`), tests L1462-1515; `commands/mod.rs:3`, `lib.rs:450-452`; delete `commands/claude_auth_probe.rs`; `commands/claude_login.rs` `plan_claude_login` L59-100 drop console/sso. `backend_provider.rs` drop `claude-max-cli`/`claude_max_cli` spellings (L58-64, L89-93). Docs: `docs/design-notes/PR-STACK-claude-track-b-tools.md`, `SPEC-claude-a2c-tool-bridging.md`, `crates/gents/proofs/README.md` (add ClaudeMap row + prose), `CLAUDE.md` L35 bullet (one Claude sentence), evidence correction note. Then PR carve per spec §8.

Gates for every code task: `cargo test -p gents` (full package, not `--lib`), `cargo check --workspace --all-targets`, `lake build` in `crates/gents/proofs` (zero sorry) when Lean touched, `tracing` not println, `escape_graphql_string`, never `[]` in a DefraDB mutation. Watch the source-scan test `provider_invocations_are_confined_to_the_owned_loop_seam` (`tests/conformance/prompt_assembly.rs:570`): new production code must not introduce `.stream(` / `.completion(` outside `loop_stream.rs` / `admission/client.rs`.

## Current file shapes worth knowing before reading

- `claude_messages.rs` (713 lines): body L74-111, `anthropic_messages` L121-194 (`Message::System` → user block `system: …` — to move into `system[]`), `parse_messages_sse` L198-288 (C1 at L223-226/L250, C8 no dup check), `mapped_tool_call` L296-322 (`unwrap_or_else(json!({}))` L317), `stream_messages` L374-444 (buffers whole body), `ClaudeMessagesTransport` L446-521, tests L523-712 (`claude_code_identity_stays_off_the_process_cli_wire` L593 uses `flatten_completion_request`/`completer_argv` → delete with the CLI wire).
- `claude_subscription.rs`: `ClaudeSeatConfig` + `from_server_flags`, seat slot, `require_process_seat`, `probe_process_seat_health` L1-130; `ClaudeSubscriptionModel::stream` routing ~L240-268.
- `claude_seat_auth.rs` (364 lines): `read_seat_access_token_at`, `parse_credentials`, Keychain L148-235, tests L244-363.
- `backend_health.rs`: Claude branch L233-262, `record_probe_event` L304-359, `run_backend_probe_cycle` L361-390, Claude tests L686-830. `backend_registry.rs` L394-405 startup ratchet skips `skips_fleet_http_probe()` kinds.
- Rendered request: `gents-protocol/src/rendered_request.rs` `CaptureSeam` L660-667, `captured_only_at` L736, test L1085-1100; `gents/src/rendered_request/mod.rs` L240-249, caller L469, test L832-858; `scope.rs` `claim_and_capture_process_cli` L381-440, tests L670-790.
- Lean: `ClaudeMap.lean` (166 lines, ends `end PromptAssembly.ClaudeMap`); `Proofs/Transcript/State.lean` `MessageRole` = user|assistant; witness cases `ContractCases/PromptAssembly.lean` L511-555.
- Loop test support: `tests/support.rs:331 echo_tool()` → `EchoTool { name: "echo", output: "ECHOED" }`; schema `AgentToolCall { tool_name tool_call_id args result lifecycle_state }`.
